page {
  background-color: #f5f5f5;
  color: #1f1f1f;
}

/* 全局样式 */
view, text, button, input, textarea {
  box-sizing: border-box;
}

button {
  margin: 0;
  padding: 0;
  border: none;
  background: none;
  line-height: inherit;
}

button::after {
  border: none;
}
